@model IEnumerable<Shopping_Appilication.Models.Size>

@{
    ViewData["Title"] = "GetAllSize";
}

<h1>GetAllSize</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.IdSize)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Status)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IdSize)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Status)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new {  id=item.IdSize  }) |
                @Html.ActionLink("Details", "Details", new {  id=item.IdSize  }) |
                @Html.ActionLink("Delete", "Delete", new {  id=item.IdSize  })
            </td>
        </tr>
}
    </tbody>
</table>
